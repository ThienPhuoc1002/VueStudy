version: "3.9"

services:
  frontend:
    build: .
    container_name: vue_app
    ports:
      - "5185:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
